/// How much of the map's left edge the rail currently covers. Shared so
/// MapLibre framing and App layout can stay in sync without importing the
/// MapLibre engine chunk.
export const railInset = { current: 0 }

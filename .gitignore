# Dependencies / build (never commit — install only where you run/tests)
**/node_modules/
node_modules/
dist/
**/dist/
.vite/
**/.vite/
coverage/
**/.cache/

# Tiles + intermediate / large data (NAS-only runtime)
*.pmtiles
*.pmtiles.old
*.mbtiles
*.osm.pbf
*.duckdb
*.duckdb.wal
data/*.geojsonseq
data/*.geojson
data/*.sqlite*
data/*.db*
# keep scripts + bbox config
!data/*.sh
!data/bbox.env
!data/*.md

# Graph / generated tooling
graphify-out/

# iOS generated / build
ios/*.xcodeproj/
ios/*.xcworkspace/
ios/DerivedData/
**/DerivedData/
**/*.xcuserstate
**/xcuserdata/

# Secrets / local env
.DS_Store
.env
.env.*
!**/.env.example
**/*.pem

# Runtime / backups
**/backups/
**/data/cache/
photon-data/
valhalla-data/
**/*.log
*.tmp
*.temp
